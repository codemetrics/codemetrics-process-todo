{
  "name": "codemetrics-process-todo",
  "version": "1.0.0",
  "description": "Detect how many TODO you have to do ",
  "main": "dist/index.js",
  "scripts": {
    "build": "babel src --out-dir dist",
    "test": "babel-tape-runner tests/* | tap-spec"
  },
  "devDependencies": {
    "babel": "^5.6.23",
    "babel-tape-runner": "^1.2.0",
    "object-assign": "^3.0.0",
    "tap-spec": "^4.0.2",
    "tape": "^4.1.0"
  }
}
